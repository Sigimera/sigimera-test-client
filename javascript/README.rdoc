= JavaScript Client

This section contains example clients written in JavaScript. Please search for
the string *CHANGEIT* and substitute it with your auth_token (see
http://api.sigimera.org/dashboard if logged in).

*ATTENTION:* This code should be used only for testing purposes. It is not
safe to use the _auth_token_ client-side.
